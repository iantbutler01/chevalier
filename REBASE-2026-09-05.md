# Upstream integration checkpoint — 2026-09-05

All eight local commits were rebased onto upstream main `4a945efdf6bbc30fdc1259af24afc3f518a86d40`. The complete original stack remains on `backup/pre-upstream-rebase-20260905` at `bece260`. No remote history was rewritten.

The combined code preserves native macOS/Windows guests and desktops, distributed controller connections, node-bound recovery policy, and lazy managed DNS startup alongside upstream's managed-provider API, execution control, fresh-boot readiness and detached VM lifecycle operations. Execution input tasks retain upstream JoinSet cancellation; VM launch serialization and local runtime teardown serialization both remain. The generated desktop target declarations retain their literal unions.

Wire compatibility: upstream `ExecStart.execution_id` is field 5; `run_as_root` now uses field 6. Update facade/VMD/embedded portproxy together when deploying. This source checkpoint does not upgrade existing guests.

Validation:

- Both `ts` and `ts-sandbox` release native builds pass.
- TypeScript/native tests: 99 pass.
- Sandbox facade: 64 unit tests and 21 contract tests pass.
- Portproxy: 31 tests pass.
- VMD: 251 tests pass with test-process-only `CHEVALIER_VMD_BACKING_FREE_FRACTION_FLOOR=0.001 CHEVALIER_VMD_BACKING_FREE_BYTES_FLOOR=1048576`; default thresholds correctly rejected writes on this nearly-full laptop. The remaining fingerprint-only test passes with an explicit synthetic hash-input file through `PROXY_BIN`. No production thresholds changed; no actual AMD64 Linux guest binary or VM boot is claimed.
- The consuming OpenBracket workspace typechecks, passes its 1,682-test root suite and 446-test Agent suite, and produces a desktop artifact whose isolated packaged API starts and serves health/catalog/notification routes.

Warnings from the existing patched fuser/platform-conditional and unused VMD helpers remain. No Kubernetes, registry, DNS or live VM changes were made during this rebase.

The integration working diff is whitespace-clean. Full-stack `git diff --check origin/main..HEAD` still reports six inherited whitespace findings in vendored Go dependencies; vendor sources were preserved unchanged.
